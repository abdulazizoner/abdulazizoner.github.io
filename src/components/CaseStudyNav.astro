---
import ThemeLanguageControls from './ThemeLanguageControls.astro';
import { pagePath } from '@/utils/base';

const projectsHome = pagePath('index.html#projects');
---

<header class="nav" role="banner">
  <div class="container case-nav">
    <div class="nav-inner">
      <a class="back-link" href={projectsHome}>
        <span data-en="← Back to projects" data-tr="← Projelere dön">← Back to projects</span>
      </a>
      <div class="nav-actions">
        <ThemeLanguageControls />
      </div>
    </div>
  </div>
</header>
